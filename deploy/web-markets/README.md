# deploy/web-markets

Markets web deploy unit (`apps/web`). See [`deploy/README.md`](../README.md).

Build-time env template: [`.env.example`](.env.example) — copy to `apps/web/.env.local` for local dev.
