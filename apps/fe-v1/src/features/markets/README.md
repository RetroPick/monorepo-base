# Markets product (web)

Polymarket-native routes under `/markets/*`. Enabled when `NEXT_PUBLIC_PRODUCT=markets` or `all`.

Legacy epoch UI lives in `../legacy/` and is excluded from Markets deploys.
