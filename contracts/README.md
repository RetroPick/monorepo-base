# Smart contracts

| Path | Status | Purpose |
|------|--------|---------|
| [`legacy-pool-v1/`](legacy-pool-v1/) | **Frozen** | Epoch-based `MarketEngine` Foundry project (formerly `package/prediction-v2`) |
| [`prism/`](prism/) | Planned | PRISM structured-outcome protocol (not started) |

Legacy contracts are **not** used by RetroPick Markets (Polymarket integration) or the Android app.

Build and test legacy contracts from the repo root:

```bash
pnpm contracts:build
pnpm contracts:test
```
