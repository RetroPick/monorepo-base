# Docker Desktop: when `postgres:5432` from containers times out (~5m migrator), use the
# published port. `extra_hosts` in docker-compose.yml defines `host.docker.internal` on Linux Engine too.
RETROPICK_COMPOSE_DATABASE_URL=postgres://retropick:retropick@host.docker.internal:5433/retropick?sslmode=disable
