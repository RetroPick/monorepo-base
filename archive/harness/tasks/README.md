# Archived harness tasks

Historical task files under `.harness/tasks/done/` describe **completed epoch-era work**. They are retained for audit only.

**Do not execute** these tasks against the live Markets V1 tree. Active work is tracked in `.harness/products/markets-v1/` and governed by `rp-*` agent personas.
