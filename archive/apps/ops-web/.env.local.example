# Copy to .env.local. The browser calls this URL (see apps/ops/lib/api.ts).
# Local dev: the API must allow your Next dev origin (any http://localhost:* or http://127.0.0.1:* port when CORS_STRICT is unset — apps/backend/internal/api/cors.go).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8080
