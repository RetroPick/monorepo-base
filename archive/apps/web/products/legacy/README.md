# Legacy epoch product (web)

Frozen MarketEngine / epoch routes under `/app/*`. Disabled when `NEXT_PUBLIC_PRODUCT=markets` or `prism`.

Source files remain under `src/views`, `src/features` until incremental migration.
