id: PR7-RV-004
title: pnpm lockfile churn audit
severity: P2
status: resolved
decision: revert_lockfile_to_main
manifest_changes:
  - packages/polymarket/src/index.ts (NodeNext .js extension only)
lockfile_audit:
  - importer: packages/polymarket
    previous: "./types" import
    new: "./types.js" import
    manifest: packages/polymarket/src/index.ts
    keep: true
  - importer: pnpm-lock.yaml
    previous: branch-regenerated resolutions
    new: origin/main lockfile restored
    manifest: none (no package.json diff vs main)
    keep: revert
verification:
  - corepack enable && pnpm install --frozen-lockfile
  - pnpm typecheck
