id: PR7-RV-004
title: pnpm lockfile churn audit
severity: P2
status: resolved
decision: sync_lockfile_to_package_json
manifest_changes:
  - packages/polymarket/src/index.ts (NodeNext .js extension)
  - apps/web/package.json unchanged vs main; lockfile importers were stale
lockfile_audit:
  - importer: apps/web
    previous: lockfile listed @retropick/contracts and @retropick/polymarket
    new: lockfile lists @retropick/polymarket only
    manifest: apps/web/package.json (unchanged)
    keep: sync required for pnpm install --frozen-lockfile
  - importer: packages/polymarket
    previous: "./types" import
    new: "./types.js" import
    manifest: packages/polymarket/src/index.ts
    keep: true
  - package: transitive resolutions
    previous: main lockfile resolution tree
    new: pnpm install normalized tree after importer fix
    manifest: derived from workspace graph
    keep: required; no package.json version bumps
verification:
  - corepack enable && pnpm install --frozen-lockfile
  - pnpm typecheck
