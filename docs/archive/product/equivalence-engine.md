# Equivalence Engine

The equivalence engine is the future home for comparing whether market, asset, feed, or template identifiers refer to the same product concept.

Current scope:

- Normalize simple string keys.
- Compare keys only inside the same scope.
- Return an explicit decision and reason.

It does not yet perform synonym matching, symbol aliasing, feed registry checks, or market deduplication.
