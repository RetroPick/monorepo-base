# @retropick/polymarket

Shared Typescript types for the Polymarket Markets BFF. Venue adapter and ingest land here in later phases.

Do not import from `@retropick/legacy/*` or epoch `@retropick/market-types`.
